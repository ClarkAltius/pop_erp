spring.application.name=backend
# Server Port
server.port=9000

# MySQL Datasource
spring.datasource.url=jdbc:mysql://localhost:3306/poperp?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=mysql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type.descriptor.sql=trace

# (delete check) can I get rid of everything here??
# ??? ?? ??
productImageLocation=C:\\poperp\\images
uploadPath=file:///C:/poperp/images/

# === ??? ?? ?? ?? ?? ===
file.upload-dir=src/main/uploads/

# === Replace with actual cloud directories in future ===

## === Image Upload Configuration (Docker/AWS Ready) ===
## 1. Where to save files on the hard drive
#productImageLocation=${APP_UPLOAD_DIR:C:/poperp/images}
#
## 2. How the URL looks when accessing the file
#uploadPath=file:${APP_UPLOAD_DIR:C:/poperp/images}/
#
## 3. Secondary upload path (unifying everything to one folder)
#file.upload-dir=${APP_UPLOAD_DIR:C:/poperp/images}/
# (delete check end)

# === Replace with actual cloud directories in future (end) ===

# for Automatic Restart ?? ???
spring.devtools.restart.enabled=true
# live reload ?? ???
spring.devtools.livereload.enabled=true

logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping=TRACE
